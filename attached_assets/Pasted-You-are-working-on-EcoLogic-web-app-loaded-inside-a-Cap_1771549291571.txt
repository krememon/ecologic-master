You are working on EcoLogic (web app loaded inside a Capacitor iOS wrapper). In the iOS simulator, the top header icons (hamburger menu, notifications bell, plus button) look visible but are NOT clickable, and the layout is pushed too high (under the notch/status bar). Fix this properly for iOS WebView safe area.

GOALS
- Ensure the app respects iPhone safe areas so interactive header buttons are not under the notch.
- Ensure hamburger / bell / plus are clickable on iOS WebView.
- Do not change the visual design beyond correct spacing.
- Must work for both mobile Safari and Capacitor WKWebView.

TASKS
1) Add/verify viewport meta supports notch safe area:
   - In the main HTML template (likely client/index.html or equivalent), ensure:
     <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />

2) Add global CSS safe-area variables and helpers:
   - Add CSS (global stylesheet) to support:
     padding-top: env(safe-area-inset-top);
     padding-left/right/bottom: env(safe-area-inset-*)

3) Update the app shell/header component:
   - Find the top navigation/header component used on mobile (hamburger, bell, plus).
   - Ensure the header wrapper has:
     - position: sticky or fixed at top
     - top: 0
     - z-index: very high (e.g., 1000+)
     - padding-top: env(safe-area-inset-top)
     - background color set (not transparent if it causes click-through issues)
   - Ensure the clickable icons/buttons have:
     - pointer-events: auto
     - and there is no parent overlay with pointer-events blocking them.

4) Detect + remove overlay blockers:
   - Inspect for any full-width overlay div (e.g., invisible layer, backdrop, drag region) covering the header area.
   - Fix by limiting overlay height, moving it below header, or setting pointer-events: none ONLY on the overlay (not on the buttons).

5) Verification steps:
   - In iOS simulator inside Capacitor wrapper, confirm:
     - hamburger opens the menu
     - bell opens notifications
     - plus opens create action
     - header is visually spaced below notch properly
   - Confirm in mobile Safari too.

DELIVERABLES
- List files changed.
- Provide before/after screenshots or describe the exact spacing fix.
- Ensure no regression on desktop layout.