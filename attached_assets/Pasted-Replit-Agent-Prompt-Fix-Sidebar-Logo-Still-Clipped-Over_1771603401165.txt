Replit Agent Prompt — Fix Sidebar Logo Still Clipped (Overflow/Height/Safe-Area)

Problem: EcoLogic logo is STILL cut off at the top of the sidebar drawer. There is visible whitespace above the hamburger, but the logo text is clipped by the top divider line. This indicates the logo row is inside a constrained container (fixed height + overflow hidden) or has negative margin/translate.

1) Find the mobile drawer sidebar component

Search for these keywords in the codebase:

SheetContent

DrawerContent

DialogContent

MobileSidebar

Sidebar

Nav

EcoLogic

logo

The issue is most likely in the mobile menu Sheet/Drawer, not the desktop sidebar.

2) Inspect the logo container and remove clipping

In the logo/header area, look for ANY of these and remove/fix them only for the header/logo row:

overflow-hidden

h-12, h-14, h-16 (fixed height too small)

-mt-* negative margin

translate-y-*

items-start with tight line-height

leading-none combined with clipping

Hard requirement: The logo row must be overflow-visible and have a safe min height.

3) Implement this exact layout structure (Tailwind)

Inside the drawer/sheet content, make the top section like this:

<div className="flex flex-col">
  {/* Header */}
  <div className="flex items-center gap-3 px-4 pt-[calc(env(safe-area-inset-top)+16px)] pb-3 overflow-visible min-h-[72px]">
    {/* Optional: hamburger can stay elsewhere if already present */}
    <div className="text-xl font-semibold leading-tight">
      EcoLogic
    </div>
  </div>

  <div className="h-px bg-slate-200" />

  {/* Nav */}
  <div className="px-3 py-3">
    {/* nav items */}
  </div>
</div>
4) If the drawer container itself is clipping, fix it

Find the wrapper around the whole drawer content (often SheetContent or a div right inside it).
If you see overflow-hidden, replace it with:

overflow-y-auto on the scrolling area

BUT keep the header overflow-visible

Example pattern:

<SheetContent className="p-0 overflow-hidden">
  <div className="h-full overflow-y-auto">
    {/* header (overflow-visible) + nav */}
  </div>
</SheetContent>
5) Fix the divider line cutting through the logo

Right now the divider appears above/overlapping the logo area.
Move the divider below the header block and ensure the header has enough padding-bottom (pb-3) and min height (min-h-[72px]).

6) Verification

Open mobile sidebar

Confirm “EcoLogic” is fully visible (no cropping)

Divider line is below the logo

No weird extra spacing changes to nav items

Works on iPhone safe-area (env safe-area inset)

Deliverable

Logo no longer clipped in the drawer/sidebar on mobile, and the divider is correctly positioned below the header.