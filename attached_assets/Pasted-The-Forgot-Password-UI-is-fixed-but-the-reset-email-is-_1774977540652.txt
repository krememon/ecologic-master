The Forgot Password UI is fixed, but the reset email is still NOT being received. We now need to debug email delivery only.

PROBLEM
The app shows success state ("Reset Link Sent"), but the actual email never arrives in the inbox.

GOAL
Find the exact root cause of why the forgot-password email is not being delivered, fix it, and verify the email is truly sent.

DEBUG THIS EXACT FLOW
1) User submits forgot password email
2) Backend receives request
3) Reset token is created and stored
4) Reset URL is built
5) Email provider is called
6) Provider accepts or rejects the send
7) Email actually reaches inbox / at least provider confirms accepted delivery

WHAT I NEED YOU TO CHECK

A. FRONTEND
- Confirm frontend is actually calling the correct forgot-password API route
- Confirm the frontend is not faking success before backend success
- Confirm response handling is truthful

B. BACKEND ROUTE
- Inspect the forgot-password API route end-to-end
- Confirm it is actually attempting to send the email
- Confirm no try/catch is swallowing the real error and still returning success
- Confirm logs show the exact path being executed

C. EMAIL SERVICE CONFIG
- Check what provider is being used for transactional email
- Verify all required env vars are present and correctly named
- Verify sender email/from address is valid
- Verify API key / SMTP credentials are actually loaded at runtime
- Verify provider is not in sandbox/test mode
- Verify the app is not pointing to a disabled or old mail config

D. DELIVERY / PROVIDER RESPONSE
- Log the exact provider response when trying to send
- If send fails, log the exact error
- If send succeeds, log the provider message ID / accepted response
- Do not just log "email sent" unless confirmed by provider response

E. RESET LINK CONTENT
- Confirm the generated reset URL is correct and production-safe
- Confirm wrong domain or malformed link is not causing hidden provider rejection

F. INBOX / DELIVERABILITY CHECKS
- Check whether emails may be landing in spam
- Check whether the sending domain/from address needs verification
- Check whether the current provider/account requires verified recipients or verified sender before sending

IMPORTANT BEHAVIOR CHANGE
- Do NOT return a fake success response if the email send actually failed
- For debugging right now, make the API/logs truthful so we know whether:
  1. token created + email send failed
  2. token created + provider accepted send
  3. request never reached real send logic
- Keep security reasonable, but right now we need truth in logs and backend behavior

ADD TARGETED LOGS
Add very clear logs for:
- forgot password request received
- normalized email
- account found or not found
- token created
- reset URL generated
- email provider selected
- send attempted
- provider success response OR full failure response
- final API response path

DELIVERABLES
1) Find the exact root cause
2) Fix it
3) List exact files changed
4) Show the exact env vars/config that were missing or wrong
5) Tell me whether the provider accepted the send
6) Give me a short test plan to verify real delivery

IMPORTANT
- Tight changes only
- Do not redesign auth
- Do not leave fake success states
- Root cause first, then fix