We need to fix the Forgot Password flow in EcoLogic end-to-end. Make these exact changes and nothing extra.

GOAL
Fix both the UI and functionality of the Forgot Password flow so it feels production-ready.

ISSUES TO FIX

1) Forgot Password page branding
- Replace the old circular ECOLOGIC droplet logo at the top of the Forgot Password screen
- Use the proper EcoLogic construction management software branding/logo instead
- Match the branding style used on the main sign in page
- The heading should visually match the sign in screen branding

2) Remove the blue info bubble
- Completely remove the blue box that says:
  "Password Reset for All Account Types..."
- Do not replace it with any other helper card or blue bubble
- Keep the page clean and simple

3) Reset email is not being sent
- The "Send Reset Link" button currently does not actually send the email
- Fix the backend so submitting a valid email triggers a real password reset email
- The email should contain a working reset link

4) Reset link flow
- When the user clicks the reset link from their email, it must open the correct reset password page
- That page must allow the user to enter a new password and save it
- After successful reset, the user should be able to sign in with the new password immediately

REQUIREMENTS

UI REQUIREMENTS
- Keep the page centered and clean
- Use the same EcoLogic logo/branding treatment as the sign in page
- Preserve the existing general layout unless needed for correctness
- Keep only:
  - logo
  - title/subtitle if appropriate
  - email input
  - Send Reset Link button
  - Back to Sign In link
- No extra explanatory text cards

BACKEND / FUNCTIONAL REQUIREMENTS
- Audit the full forgot-password pipeline:
  - frontend submit handler
  - API route
  - reset token creation
  - token persistence
  - email sending
  - reset-password form
  - password update logic
- Ensure token links use the correct public base URL logic
- Use APP_PUBLIC_BASE_URL if available, otherwise safe fallback
- Do NOT generate localhost or wrong-domain links in production
- Make sure the token expires properly
- Make sure passwords are securely updated using the app’s existing auth/password hashing flow
- If account exists, the request should succeed in a safe production-style way
- Avoid leaking whether an email exists, unless current product behavior intentionally does that already

EMAIL REQUIREMENTS
- The reset email must actually send
- Subject should be clear, like: Reset your EcoLogic password
- Email body should include a button or link to reset password
- The link must land on the EcoLogic reset password page
- Keep email simple and branded if existing email system already supports branding

DEBUGGING REQUIREMENTS
- Add targeted logs only for the forgot-password flow so we can confirm:
  - request received
  - token created
  - reset URL generated
  - email send attempted
  - email send success/failure
  - password reset success
- Do not add noisy unrelated logs

DELIVERABLES
1) Fix the Forgot Password page UI
2) Fix the backend/email sending so reset emails actually send
3) Fix the reset link page so user can set a new password
4) Confirm exact files changed
5) Explain root cause of why emails were not being sent
6) Provide a short manual test plan for:
   - request reset
   - receive email
   - open reset link
   - change password
   - sign in with new password

IMPORTANT
- Do not touch unrelated auth flows
- Do not redesign the whole auth system
- Keep changes tight and production-safe
- Preserve existing branding elsewhere