UI polish request: The "Change Password" (Send Reset Link) modal works, but it looks outdated and spaced weird. I want it cleaned up and modern to match the style of the "Select Customer" modal.

DO NOT change any backend logic or functionality. Only update UI/spacing/typography.

Match these style cues from Select Customer modal:
- Clean centered title row with an X close button on the right
- Subtle subtitle under title (smaller, muted)
- Tighter spacing (less empty vertical space)
- Rounded modal corners consistent with the app
- Inputs/buttons aligned and sized consistently
- Buttons should look modern (same height, rounded, good padding)
- No giant icon dominating the top; keep icon subtle or remove

Target modal: Change Password / Send Reset Link modal

Specific changes:
1) Header
- Replace current big icon + left aligned header with:
  - Title: "Reset Password" (or "Change Password" if preferred)
  - Subtitle: "We’ll email you a secure link to set a new password."
  - Close (X) in top-right, same as Select Customer modal.

2) Body
- Show a clean label like:
  "Send reset link to"
- Then show the email in a bordered, non-editable pill/field (same style as a disabled input), not just plain text.
- Remove excessive whitespace between elements.

3) Footer buttons
- Keep two buttons side by side:
  - Left: Cancel (secondary outline)
  - Right: Send Reset Link (primary)
- Make them same height, same width, with consistent rounded corners.
- Disable the Send button while loading and show a small spinner or "Sending..." state.
- Avoid toast popups if you currently use them; inline success in modal is fine.

4) Success state (after sending)
- Keep it clean:
  - Small green check icon (optional, not huge)
  - Title: "Email sent"
  - Text: "Check your inbox at <email>. Link expires in 1 hour."
  - Single button: "Done"
- Make spacing tight and visually consistent.

Deliverables:
- Only UI changes to the modal component + shared modal styles if needed
- Confirm no functional logic changed
- Confirm it still sends reset email and closes properly
