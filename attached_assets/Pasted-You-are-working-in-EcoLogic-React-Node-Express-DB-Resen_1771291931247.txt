You are working in EcoLogic (React + Node/Express + DB + Resend).  

GOAL:
After a payment is successful (cash/check/stripe), and AFTER the signature is captured, automatically email the client the EXACT SAME PDF that is produced by the "Generate Invoice PDF" modal for that invoice/job. Do NOT email the receipt HTML page. Do NOT generate a different alternate invoice template. It must be the identical PDF artifact from the manual "Generate Invoice PDF" flow.

CURRENT PROBLEM:
We are emailing something else (receipt HTML / confirmation), not the PDF file that the invoice PDF generator creates.

REQUIREMENTS:
1) Signature is ALWAYS required after payment (already done). Email triggers ONLY after signature is saved successfully.
2) Works for ALL payment methods:
   - Manual cash/check payments (payment created immediately)
   - Stripe payments (payment created via webhook)
3) The attachment must be the invoice PDF generated by the "Generate Invoice PDF" flow for that invoice:
   - If the PDF already exists for the invoice, use it.
   - If it does NOT exist yet, generate it USING THE SAME generator used by the "Generate Invoice PDF" modal, then use that generated file (and persist it so future sends reuse it).
4) Attach the PDF to the email (Resend attachment), and optionally include a link to view/download the same PDF file.
5) Prevent duplicates:
   - Only send once per paymentId (or invoiceId) after signature.
   - If user retries signature save, do not resend.
6) No UI popups/toasts for "Event created/deleted" etc are unrelated — ignore. Focus only on receipt email attachment behavior.

IMPLEMENTATION PLAN (DO THIS):
A) Persist the actual generated PDF reference for an invoice
- Find the backend endpoint used by the "Generate Invoice PDF" modal (ex: POST /api/invoices/:id/pdf or similar).
- Ensure it saves a stable record in DB like:
  invoice_pdf_generations table:
    id, invoiceId, storagePath, publicUrl, createdAt, createdByUserId, hash(optional)
  or add columns on invoices:
    invoices.pdfStoragePath, invoices.pdfUrl, invoices.pdfGeneratedAt
- The key is: there must be a single source-of-truth pointer to "the PDF that the modal generated".

B) Add "receipt sent" idempotency tracking
- Add a DB table:
  payment_receipts
    id, paymentId (unique), invoiceId, emailedTo, sentAt, resendMessageId(optional)
- Or add on payments:
  payments.receiptSentAt, payments.receiptResendId
- Use a UNIQUE constraint on paymentId to prevent duplicates.

C) Trigger email after signature is saved
- Locate POST /api/payments/:paymentId/signature
  This is where signature is stored today.
- After successfully saving signature:
  - Load payment + invoice + job + customer
  - Determine recipient email from the customer record tied to the invoice/job.
  - Resolve invoice PDF:
     1) If invoice has pdfUrl/pdfStoragePath (or has a row in invoice_pdf_generations) use the latest one.
     2) If none exists, call the SAME PDF generator function used by the modal, generate it, persist it, then use it.
  - Send email via Resend with PDF attached:
     - Read file bytes from storage (local uploads or whatever you use)
     - Attach as:
       filename: `Invoice_${invoiceNumber}.pdf`
       content: base64
  - Record payment_receipts row / payments.receiptSentAt so we never resend.

D) Stripe flow must work too
- Stripe webhook creates the payment record.
- The signature occurs after the user returns and signs.
- Since we trigger email from the signature endpoint, Stripe payments will work automatically as long as the signature endpoint uses the paymentId created by webhook (which it does).
- Make sure the UI uses the correct paymentId for Stripe payments when collecting signature (if it currently doesn’t, fix it):
  - After /stripe/return -> when loading the job/invoice, fetch the latest payment for that invoice and use that paymentId for signature capture.

E) Use EXACT same PDF as the modal
- Do NOT build a separate HTML invoice template for email.
- Reuse the existing PDF generator codepath used by the modal.
- The email attachment MUST be that same artifact (same stored file).

DELIVERABLES:
1) DB migration(s) for storing invoice PDF reference + receipt-sent tracking.
2) Backend changes:
   - PDF generation endpoint persists pdf path/url.
   - Signature endpoint triggers email with correct PDF attachment and idempotency.
3) Frontend:
   - Ensure signature capture after Stripe uses correct paymentId (latest payment for invoice).
4) Logs (targeted):
   - When signature saved: log paymentId, invoiceId, whether pdf reused vs generated, and whether email sent or skipped (already sent).
5) Tests checklist:
   - Manual CASH payment -> signature -> verify email arrives with PDF that matches "Generate Invoice PDF" output.
   - Manual CHECK payment -> same.
   - Stripe payment -> return -> signature -> email arrives with correct PDF.
   - Repeat signature save -> no duplicate email.

IMPORTANT:
- Do NOT change the overall UI design.
- Do NOT email the receipt page HTML.
- MUST attach the actual PDF file.
