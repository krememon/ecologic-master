# EcoLogic Construction Management Platform

## Overview
EcoLogic is a professional construction management platform designed for trade contractors to centralize job management, streamline subcontractor coordination, enhance client communication, and manage invoicing. The platform aims to modernize construction workflows, improve project oversight, and foster better stakeholder communication through a real-time, PWA-enabled web application. The business vision is to innovate construction project management by leveraging advanced technology and user-centric design to maximize efficiency and profitability for contractors.

## User Preferences
Preferred communication style: Simple, everyday language.
Design preferences: Bold, uppercase "EcoLogic" branding with wide letter spacing
Black Screen Fix: Global fetch interceptor in main.tsx auto-attaches Bearer token to all same-origin fetch calls. HTML/CSS base layer ensures background color is always visible before React mounts.
Authentication: Quad authentication options (Email/Password + Replit + Google OAuth + Apple Sign-In)
Registration: Enhanced form with password strength meter and show/hide toggles
User Flow: Automatic login and dashboard redirect after account creation
Logo: Custom water drop with leaf logo positioned above EcoLogic title

## System Architecture
EcoLogic is a multi-tenant web application. The frontend is built with React 18 (TypeScript, Vite, Tailwind CSS with shadcn/ui, TanStack Query, Wouter, React Hook Form). The backend uses Node.js with Express.js (TypeScript) and PostgreSQL with Drizzle ORM.

**UI/UX**:
- Responsive design with PWA support and dark mode.
- Accessible components primarily from Radix UI, incorporating custom "EcoLogic" branding and logo.

**Technical Implementations**:
- **Authentication**: Supports Replit Auth, email/password, and social logins (Google, Apple) with session management and secure cookie handling.
- **Data Management**: PostgreSQL with Drizzle ORM, providing multi-tenancy and role-based access control (RBAC).
- **Real-time Capabilities**: WebSocket server for real-time communication.
- **Push Notifications**: Supports iOS native push notifications via APNs HTTP/2.
- **Messaging System**: Comprehensive two-pane interface for 1:1 conversations with real-time delivery and read receipts.
- **File Management**: Folder-based file cabinet system for document organization, upload, and management.
- **Document Visibility**: Role-based document visibility rules enforced at the API level.
- **Signature Requests**: Electronic signature system with RBAC, secure access tokens, and branded email notifications.
- **Employee Management**: Manages employee status and contact information.
- **Onboarding**: Invite code system for company onboarding, including owner registration, member joining, and industry-specific Price Book seeding.
- **Subscription Management**: Team size-based plan auto-selection and subscription gating.
- **Apple IAP Backend (Phase 2A)**: StoreKit 2 JWS transaction verification via `jose` x5c chain. `POST /api/subscriptions/validate` accepts `{ platform: "apple", jwsTransaction }`. `POST /api/apple/notifications` handles App Store Server Notifications v2 (renewal, expiration, revocation). Product ID mapping in `shared/subscriptionPlans.ts` (`appleProductId` + `appleProductIdToPlanKey`). Verification module at `server/appleIap.ts`.
- **Native IAP Frontend (Phase 2B — iOS + Android)**: `OnboardingSubscription.tsx` and `Paywall.tsx` use a three-way platform branch (nativeIos / nativeAndroid / web). `client/src/lib/nativeIap.ts` exports helpers for both platforms using `@capgo/native-purchases`. iOS: `isNativeIos()` → `loadAppleProducts()` → `purchaseAppleSubscription()` → JWS → validate. Android: `isNativeAndroid()` → `loadGooglePlayProducts()` → `purchaseGooglePlaySubscription()` → `{ purchaseToken, productId }` → validate. Restore mirrors the purchase path for each platform. Web/Stripe flow is completely unchanged.
- **Google Play IAP Backend**: `POST /api/subscriptions/validate` also accepts `{ platform: "google_play", purchaseToken, productId }`. Validates via Google Play Developer API using a service account JWT bearer flow (requires `GOOGLE_PLAY_SERVICE_ACCOUNT_KEY` env var). `POST /api/google/notifications` handles Google Cloud Pub/Sub RTDN webhooks (purchased, renewed, expired, revoked, on-hold, etc). Product ID mapping in `shared/subscriptionPlans.ts` (`googlePlayProductId` + `googlePlayProductIdToPlanKey`). Verification module at `server/googlePlayIap.ts`. `subscriptionPlatform` column distinguishes `apple`/`google_play`/`stripe`. The `billingResolver.ts` source union includes `google_play`.
- **Estimates System**: Job-scoped estimate creation with line item editor, unique numbering, and RBAC.
- **Customer Management**: Manages customer data for estimate recipients.
- **Price Book (Service Catalog)**: Owner-only customizable reusable line item templates with full CRUD operations and RBAC.
- **Company Profile**: Owner-only feature for managing company identity and branding.
- **Tax Management**: Owner-only feature for creating custom tax rates applied to invoices.
- **Payment Collection**: Supports multi-method payment collection (Cash, Check, Card via Stripe) with idempotent processing, job-scoped RBAC, and discount features.
- **Payment Receipt Emails**: Automatic receipt emails sent to customers post-payment with PDF invoice attachments.
- **Refund System**: Manual refund recording with RBAC, updating invoice totals.
- **Payments Tracker**: Dashboard offering a real-time financial overview with filterable invoices and support for partial payments.
- **Payment Signature Capture**: Configurable post-payment signature collection.
- **Invoice Sending**: Invoices can be sent via Email (Resend) or SMS (Twilio) with payment links.
- **Native PDF Share (iOS)**: On native iPhone, tapping the download button on invoice/estimate PDFs fetches the file, writes it to the app cache via `@capacitor/filesystem`, and opens the native iOS share/save sheet (`@capacitor/share`) so users can save to Files, Notes, AirDrop, etc. Web keeps standard anchor-download behavior.
- **Leads Management**: Dedicated section for tracking potential customers with RBAC.
- **Time Tracking**: Job-aware clock-in/out for technicians, aggregated "Labor Today" for managers, and auto clock-out.
- **Internal Dashboard — Campaign Tracking**: Private internal dashboard at `staging-dashboard.ecologicc.com` (admin-only, dual-domain split from the customer app via `client/src/main.tsx` host detection). Implements first-touch attribution for marketing campaigns. Customer app captures `?ref` / `?source` / `?campaign` URL params on every load via `client/src/lib/attribution.ts` (90-day TTL, persisted to both `localStorage` and a cookie, skipped on dashboard hosts so admins are never attributed). Onboarding (`OnboardingCompany.tsx`) prefills source + referral fields from saved attribution and POSTs them to `/api/companies`, which (in a non-blocking try/catch) matches the referral code to an active row in `growth_campaigns` and writes a `growth_subscribers` row via `saveOrKeepSubscriberAttribution` (first-touch wins — never overwrites existing source/code/campaign). Dashboard pages: `Campaigns.tsx` (full CRUD with hostname-aware tracking-link generator → `staging.ecologicc.com` vs `app.ecologicc.com`), `Subscribers.tsx` (filterable view with campaign/source/platform/status), `Sources.tsx` (per-source-type aggregation with revenue + canceled counts), `Platforms.tsx` (per-platform aggregation: Stripe / Apple / Google Play / Manual / Unknown — backed by `getPlatformBreakdown()` and `GET /api/admin/dashboard/platforms`), `Accounts.tsx` (every customer company — attributed or not — with side-drawer for editing internal status / notes / attribution, on-demand Stripe subscription refresh, and a destructive "Delete Account" flow that permanently wipes the company + every per-company row across the system in a single DB transaction; backed by `growth_account_admin` table for admin-only metadata that does NOT block customer-app access). Source-type catalog is single-sourced in `shared/growthSources.ts`. Logging prefixes: `[attribution]` (capture + onboarding save), `[dashboard-campaigns]` (admin CRUD), `[dashboard-accounts]` (account admin actions including delete), `[growth-dashboard]` (Stripe / Apple / Google Play sync into `growth_subscribers`).
- **Growth-Subscribers Multi-Platform Sync**: `growth_subscribers` is the all-in-one subscriber table behind every dashboard page (Subscribers / Sources / Platforms / Overview). Three sync entrypoints all live in `server/dashboard/storage.ts` and follow the same upsert-or-create pattern (match by companyId → userId → platform-specific transaction id; preserve any existing first-touch attribution; backfill empty owner/company/signup fields on hit; on miss CREATE a new row with `sourceType = "unknown"` and `platform` set to the originating platform). MRR rules are identical across platforms: trial = `$0`, active/past_due = plan price from `shared/subscriptionPlans.ts`, canceled/expired = `$0`. Lifecycle stamps (`trialStartedAt`, `becamePaidAt`, `canceledAt`) are written once and never overwritten. (1) **Stripe** — `syncStripeSubscriptionToGrowthSubscriber` is invoked from the existing webhook hooks in `server/index.ts` (`customer.subscription.{created,updated,deleted}`); unattributed signups now produce a `sourceType=unknown / platform=stripe` row instead of being silently dropped. (2) **Apple** — `syncAppleSubscriptionToGrowthSubscriber` is invoked from the Apple branch of `POST /api/subscriptions/validate` after `storage.updateCompany` succeeds; matches on `appleOriginalTransactionId` / `appleTransactionId` and writes `platform=apple`. Status mapping via `mapAppleStatusToGrowth` (Apple `subscriptionStatus` 1=active, 2=expired, 3=past_due, 4=expired, 5=canceled). (3) **Google Play** — `syncGoogleSubscriptionToGrowthSubscriber` is invoked from the Google branch of the same validate endpoint; matches on `googlePurchaseToken` / `googleOrderId` and writes `platform=google_play`. Status mapping via `mapGoogleStatusToGrowth` using Google `paymentState` + `autoRenewing` (paymentState=2 → trialing, paymentState=1 + autoRenewing → active, paymentState=1 + !autoRenewing → canceled, etc). All three sync paths run inside best-effort try/catch so a sync failure never breaks the underlying validate / webhook response. Logging prefix `[growth-dashboard]` for every create/update.
- **Custom Smart-Link Redirector (Dashboard, staging-first)**: In-house replacement for Branch.io smart links — same UX (`go.ecologicc.com/:referralCode` in prod, `staging-go.ecologicc.com/:referralCode` in staging) at $0/mo. Implementation lives in `server/smartLinks.ts` (`getSmartLinkConfig`, `detectPlatform`, `recordClick`, `pickRedirectTarget`, `mountSmartLinkRoutes`) and is wired in at the **top** of `registerRoutes` in `server/routes.ts` so it runs before the SPA catch-all. Two surfaces: (1) a universal path-style route `GET /go/:referralCode` mounted on every host (handy for Replit preview + manual links), and (2) a hostname-aware Express middleware that intercepts requests whose `req.hostname` matches `SMART_LINK_DOMAIN` (env override, comma-separated allowed) **or** the built-in defaults `go.ecologicc.com` / `staging-go.ecologicc.com`, treats the entire path as the referral code, and short-circuits the rest of the stack. Both paths skip `/api/`, `/uploads/`, `/public/`, `/.well-known/`, `/__replco/`, favicons, robots, sitemap, and any path with a file extension so static assets and APIs are untouched. On every request: lookup `growth_campaigns` by `referralCode` (case-insensitive), parse User-Agent into `ios` / `android` / `web` / `unknown`, best-effort INSERT into `growth_mobile_events` (`event_type='click'`, `platform`, `userAgent`, `referrer` — `campaignId` only set when matched, so unknown codes are still logged for debugging), then 302 redirect with `Cache-Control: no-store`. Redirect target priority: matched campaign + iOS UA + `APP_STORE_URL` set → App Store; matched + Android UA + `PLAY_STORE_URL` set → Play Store; everything else → `SMART_LINK_WEB_BASE_URL/signup?source=<sourceType>&ref=<code>` (defaults to `https://staging.ecologicc.com/signup` on staging hosts, `https://app.ecologicc.com/signup` everywhere else). Unknown referral codes still hit the web signup with `?ref=<code>` preserved so attribution can fire on signup. Dashboard `Campaigns.tsx` renders the smart link as the **primary** mobile-link control (Link2 = copy, ExternalLink = open) using a hostname-aware `buildSmartLink()` helper — when the dashboard host matches a real smart-link domain it builds `https://go.ecologicc.com/<code>`, otherwise it falls back to the path-style `https://<current-host>/go/<code>` so the link works in Replit preview too. Public config is exposed through `GET /api/admin/dashboard/smart-link/config` (admin-gated; returns `smartLinkDomain`, `webBaseUrl`, `appStoreUrlConfigured`, `playStoreUrlConfigured`). Required env vars (all optional with sensible defaults): `SMART_LINK_DOMAIN` (default `go.ecologicc.com,staging-go.ecologicc.com`), `SMART_LINK_WEB_BASE_URL`, `APP_STORE_URL`, `PLAY_STORE_URL`. Production rollout requires DNS: `CNAME staging-go.ecologicc.com → <Replit deployment hostname>`. Logging prefixes: `[smart-link]` (redirector), `[dashboard-smart-link]` (admin config endpoint).
- **AppsFlyer OneLink Mobile Attribution (Dashboard, Phase 1, staging-only)**: Primary mobile attribution path going forward — replaces the paused Branch flow with AppsFlyer OneLink at no incremental dashboard cost. Phase 1 ships dashboard-only: every campaign automatically gets a working OneLink URL the moment `APPSFLYER_ONELINK_TEMPLATE_ID` + `APPSFLYER_ONELINK_DOMAIN` are configured, with optional per-campaign manual override. Three building blocks: (1) **Schema** — single nullable column `appsflyerOneLinkUrl` (varchar 500) on `growth_campaigns`. Empty/null means "use the auto-derived URL"; populated means "use this exact URL" (typically a branded short link minted in the AppsFlyer dashboard). No event tables added in Phase 1 — `growth_mobile_events` is reused later in Phase 3 for click/install/open webhooks. (2) **Server helper** in `server/appsflyer.ts` (`getAppsflyerPublicConfigSummary`, `isAppsflyerOneLinkConfigured`, `isAppsflyerApiConfigured`, `buildOneLinkUrl`) — pure config probe + URL builder, no network calls in Phase 1. URL format: `https://<APPSFLYER_ONELINK_DOMAIN>/<APPSFLYER_ONELINK_TEMPLATE_ID>?af_xp=custom&pid=<sourceType>&c=<referralCode>&deep_link_value=<referralCode>&deep_link_sub1=<sourceType>&deep_link_sub2=<campaignId>&deep_link_sub3=<campaignName>&deep_link_sub4=<sourceName>`. (3) **Routes** — admin `GET /api/admin/dashboard/appsflyer/config` (env-presence probe, no secret values returned). Campaign create/update routes already accept arbitrary insert-schema fields, so the new `appsflyerOneLinkUrl` column flows through `formToPayload` → existing storage layer with no route-level changes. Dashboard `Campaigns.tsx` adds: a `buildAppsflyerLink(c, config)` helper mirroring `buildSmartLink`'s shape (custom paste wins → else compute from config + campaign), violet Smartphone/ExternalLink row buttons (`copy-appsflyer-link-${id}` / `open-appsflyer-link-${id}`) with `[campaign-links] copy/open appsflyer link` console logs, and a new "AppsFlyer OneLink URL (optional)" form input + violet preview line in the create/edit dialog. Phase 2 (mobile SDK) and Phase 3 (webhook → `growth_mobile_events`) are explicitly deferred. Required staging env vars (server): `APPSFLYER_DEV_KEY`, `APPSFLYER_APP_ID_IOS` (or legacy `APPSFLYER_IOS_APP_ID`), `APPSFLYER_APP_ID_ANDROID`, `APPSFLYER_ONELINK_TEMPLATE_ID`, `APPSFLYER_ONELINK_DOMAIN`, optional `APPSFLYER_API_TOKEN` (for future API-based link minting). Required Vite-baked vars (mobile native init in `client/src/lib/appsflyer.ts`, already present): `VITE_APPSFLYER_DEV_KEY`, `VITE_APPSFLYER_IOS_APP_ID` (legacy alias to `VITE_APPSFLYER_APP_ID_IOS`), `VITE_APPSFLYER_APP_ID_ANDROID`. Logging prefixes: `[appsflyer]` (server helper), `[dashboard-appsflyer]` (admin config endpoint), `[campaign-links]` (dashboard row + dialog interactions), `[appsflyer-attribution]` (reserved for Phase 2 deferred-deep-link save).
- **Branch.io Deferred Deep Links (Dashboard, dormant)**: Per-campaign mobile attribution code from the original Phase 1 build — **fully wired but disabled at runtime** because Branch's $199/mo pricing didn't fit. The custom smart-link redirector above replaces day-to-day usage, but the Branch code path is preserved verbatim so we can re-enable it in minutes if a future plan justifies it. Three building blocks: (1) **Schema** — Branch fields added to `growth_campaigns` (`branchLinkUrl`, `branchLinkId`, `branchAlias`, `mobileTrackingEnabled`, `branchChannel`, `branchFeature`, `branchCampaign`, `branchCreatedAt`, `branchUpdatedAt`) plus a new `growth_mobile_events` event log keyed by `branchEventId` with FILTER pivots for clicks/installs/opens; (2) **Branch HTTP client** in `server/branch.ts` (`createBranchLink`, `deleteBranchLink`, `verifyWebhookSecret`, `parseBranchWebhookPayload`, `getBranchPublicConfigSummary`) — talks to `POST https://api.branch.io/v1/url` with `branch_key` in the body, supports custom domain via `BRANCH_LINK_DOMAIN`, alias = lowercased referral code, `overwrite=true` always passed on regenerate, and a PII sanitizer that strips email/ip/user_agent/idfa/gaid before persisting raw payloads; (3) **Routes** — admin `POST /api/admin/dashboard/campaigns/:id/branch-link` (gated by `requireAuth + requireDashboardAdmin` AND `BRANCH_INTEGRATION_ENABLED=true` AND `BRANCH_KEY` set, idempotent on re-run, returns updated campaign + mobile metrics), admin `GET /api/admin/dashboard/branch/config` (env-presence probe with no secret values returned), and a public `POST /api/webhooks/branch` mounted *before* the gate array so it bypasses dashboard auth — webhook is fail-closed: `BRANCH_WEBHOOK_SECRET` env var must match either the `X-Branch-Webhook-Secret` header or the `?token=` query param via constant-time comparison. Webhook handler is idempotent on `branchEventId` (unique index → 23505 swallowed as duplicate) and always returns 200 once auth passes so Branch doesn't aggressively retry. Dashboard `Campaigns.tsx` has a **"Enable mobile install tracking with Branch"** checkbox in the create/edit form (off by default), a **Mobile** column showing `clicks · installs · opens`, and three new row buttons (Smartphone/Copy = copy mobile link, RefreshCw = regenerate, Smartphone outlined = generate first time) — all disabled with a helpful tooltip when the server isn't configured. **Mobile-native Capacitor SDK is intentionally deferred** — Phase 1 generates and serves Branch links from the web flow only; the iOS/Android wrapper hasn't been wired to the Branch SDK yet, so install/open events will only flow once the mobile app integration ships in a follow-up. Required staging env vars: `BRANCH_INTEGRATION_ENABLED=true`, `BRANCH_KEY`, `BRANCH_SECRET` (delete only), `BRANCH_LINK_DOMAIN=go.ecologicc.com`, `BRANCH_IOS_FALLBACK_URL`, `BRANCH_ANDROID_FALLBACK_URL`, `BRANCH_WEBHOOK_SECRET`. Branch dashboard must point its webhook at `https://staging-dashboard.ecologicc.com/api/webhooks/branch?token=$BRANCH_WEBHOOK_SECRET`. Logging prefixes: `[branch]` (HTTP client), `[dashboard-branch]` (admin route), `[branch-webhook]` (public webhook).
- **Account Deletion (Dashboard)**: Hard-delete of a customer company is exposed through the Accounts drawer at `staging-dashboard.ecologicc.com` and gated by **four** layers: (1) `requireAuth + requireDashboardAdmin` route gate, (2) env-var `ALLOW_DASHBOARD_ACCOUNT_DELETION=true` (default OFF — staging-only), (3) confirmation payload `{ confirmText: "DELETE", understood: true }` + matching modal that disables the submit button until the admin types `DELETE` AND ticks the "I understand" checkbox, and (4) hardcoded protected list inside `server/adminDeleteService.ts` (currently `pjpell077@gmail.com` / company 415). Self-delete (admin is a member of the target company) is also blocked unless `ALLOW_SELF_ACCOUNT_DELETE=true`. The deletion runs `deleteCompanyDeep()` inside a single `db.transaction`, wiping ~33 tables — including `growth_subscribers` and `growth_account_admin` — and then performing **full auth-record cleanup** so the deleted account's email becomes immediately reusable for re-signup. Auth cleanup classifies every candidate user (company owner + every `company_members.user_id`) into one of three buckets — *willDelete* (orphan, not protected), *willKeepBecauseOtherCompany* (still belongs to another company), *willKeepBecauseProtected* (email is in `PROTECTED_OWNER_EMAILS` or the runtime `DASHBOARD_ADMIN_EMAILS` allow-list, or the user is the actor running the delete) — then for the *willDelete* bucket removes rows from `push_tokens`, `login_challenges`, `pending_signups` (matched by `LOWER(email)` since this table has no `user_id`), best-effort `sessions` (substring match on the JSONB `sess` column scoped to those user-ids), and finally `users`. The same classification runs read-only inside `previewAccountDeletion()` so the confirmation modal can show admins exactly which user accounts will disappear, which will be kept (and why), and how many `pending_signups` rows will be cleared, **before** they type DELETE. Stripe / Apple / Google subscriptions are **NOT** auto-canceled — local references are removed and the modal warns the admin to cancel external billing separately. Every delete writes an `admin_audit_logs` row (`actorEmail`, `targetType=company`, `targetId`, `targetName`, `beforeValue` JSON containing per-table counts + tablesAffected + `deletedUserEmails` + `keptUsers` + `authIdentitiesDeleted` + `sessionsDeleted` + warnings). Endpoints: `POST /api/admin/dashboard/accounts/:companyId/delete-preview` (read-only counts + per-user breakdown for the modal) and `DELETE /api/admin/dashboard/accounts/:companyId` (the destructive call). Logging prefix `[dashboard-accounts] …`.
- **Geo-Tracking**: GPS tracking tied to clock-in/clock-out, with live crew positions shown on the Schedule map. Android uses a native foreground service (`LocationService.java`) with FusedLocationProviderClient for true background tracking; iOS uses `@capacitor/geolocation` watchPosition; web uses `navigator.geolocation`. Custom Capacitor plugin `LocationTrackingPlugin.java` bridges JS↔native. Tracking state persisted in `localStorage` (`geoSessionId`) to survive app restarts while clocked in. Auth forwarded to service via Bearer token from `nativeSessionId`.
- **Timesheet Editing**: Manager-only timesheet editing with audit trails and RBAC.
- **Archival**: Automatic archival for estimates and jobs with a restore option.
- **Schedule Month View**: Full monthly calendar grid with job/estimate/event indicators and navigation.
- **Schedule Events**: Manager-created company calendar events with RBAC-enforced visibility and CRUD.
- **Notifications System**: In-app notification system with various types, unread badges, and deep linking.
- **QuickBooks Online Integration**: Owner-only QBO integration for customer mapping and invoice/payment synchronization.
- **Bulk Email/SMS Campaigns**: Bulk promotional messaging to customers with multi-select, preview, and opt-in management.
- **Email Unsubscribe System**: Secure one-click unsubscribe for marketing emails.
- **Plaid Bank Connection**: Owner-only Plaid Link integration for connecting company bank accounts.
- **Stripe Connect**: Full subcontractor payout infrastructure with dual-transfer architecture, where EcoLogic acts as a payment router. Includes onboarding gate and owner-facing payout audit UI.
- **Security Hardening**: Cross-tenant data isolation, centralized RBAC, secure storage, rate limiting, and log sanitization.
- **Native Wrapper Google OAuth**: System-browser flow for Capacitor iOS/Android wrappers using deep links.
- **Stripe Payment Element**: In-app card payments using Stripe PaymentIntents, embedded directly without redirection.
- **Invoice Payment Recompute**: Invoice totals are dynamically computed from payment records for accuracy.
- **Contractor Network – Job Referrals**: Contractor-to-contractor job referral system with status tracking, referral fees, and invite mechanisms.
- **Job Offer Deep Links**: Universal Links (iOS) and App Links (Android) for job offer paths, with fallback landing pages.
- **Legal Pages**: Settings hub with Terms of Service and Privacy Policy.
- **Support System**: Settings hub for contacting support, reporting bugs, requesting features, and FAQs.
- **Admin Tools**: Previous Dev Tools implementation fully removed. Shared infrastructure kept: `server/devAuth.ts` (`isDevAccount` used by subscription bypass), `server/billingResolver.ts` (`getEffectiveBillingAccess` used by `/api/subscriptions/status`). Admin override columns on companies table and `admin_audit_logs` table remain in DB schema. Rebuild pending.

**Mobile App (React Native + Expo)**:
- Native iOS/Android app with `expo-secure-store` for session management.
- **Location Tracking**: Utilizes `expo-location` and `expo-task-manager` for background GPS tracking.
- **Schedule Screen**: Features a list/map toggle for job visualization with real-time location polling.

## External Dependencies
- **Database**: PostgreSQL (via Neon serverless)
- **ORM**: Drizzle ORM
- **Authentication**: Passport.js, Replit Auth, Google OAuth, Apple Sign-In (`apple-signin-auth`)
- **Email**: Resend
- **SMS**: Telnyx, Twilio
- **File Uploads**: Multer
- **WebSockets**: `ws` library
- **UI Libraries**: Tailwind CSS, shadcn/ui, Radix UI, Framer Motion
- **Payments**: Stripe
- **Bank Integration**: Plaid